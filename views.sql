Create view vistapred
as (query)
select *
from database_nombre y los joins necesarios
where XXX

alter vistapred (col1, col2, col3, col4)
as
select
       (col11, col22, col33, col44)
	   from database_nombre
